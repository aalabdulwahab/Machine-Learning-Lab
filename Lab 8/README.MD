This project implements the KNN algorithm for classification tasks.  
It includes data preprocessing, model training, and evaluation.  
The goal is to analyze how K affects model performance.  
Built using Python and scikit-learn.
